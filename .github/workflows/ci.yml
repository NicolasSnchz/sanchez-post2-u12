# Este pipeline se configurara completamente en el Post Contenido 2.
name: CI pendiente

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI se configura en Post Contenido 2"
